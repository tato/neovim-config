return {
    ensure_installed = { "zig", "toml", "glsl", "lua", "vim", "html", "javascript", "css" },
    sync_install = true,
    highlight = {
        enable = true,
        additional_vim_regex_highlighting = true,
    },
}
